'use client';

import React from 'react';

type HeaderProps = {};

export default function Header({}: HeaderProps) {
  return null; // Убираем заголовок, чтобы избежать его появления во время загрузки
}
